<p id="notice"><%= notice %></p>

<!--<h1><%= t('view.title') %></h1>-->
<h1>掲示板</h1>

<table>
  <!--<thead>-->
  <!--  <tr>-->
  <!--    <th>Text</th>-->
  <!--    <th>Contributor</th>-->
  <!--    <th>Email</th>-->
  <!--    <th colspan="3"></th>-->
  <!--  </tr>-->
  <!--</thead>-->

  <tbody>
    <% @contributions.each do |contribution| %>
      <tr>
        <td>
          <span class="contribution-id"><%= contribution.id %></span>
          <span class="contributor"><%= contribution.contributor %></span>
        </td>
        <td><%= contribution.email %></td>
        <td><%= contribution.created_at.strftime("%Y/%m/%d(#{%w(日 月 火 水 木 金 土)[contribution.created_at.wday]}) %H:%M:%S") %></td>
      </tr>
      <tr>
        <td colspan=3><%= simple_format(h(contribution.text)) %></td>
      </tr>
      <tr>
        <td class="actions-link" colspan=3>
          <%= link_to 'Edit', edit_contribution_path(contribution) %>
          <%= link_to 'Destroy', contribution, method: :delete, data: { confirm: 'Are you sure?' } %>
        </td>
      </tr>
      <tr>
        <td colspan=3><hr></td>
      </tr>
    <% end %>
  </tbody>
</table>

<div id="paginate">
  <%= paginate @contributions %>
</div>
<!-- <%= link_to_next_page @contributions, 'more', remote: true, id: 'more_link' %> -->

<div>
  <%= render 'form' %>
</div>
