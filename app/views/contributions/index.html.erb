<p id="notice"><%= notice %></p>

<!--<h1><%= t('view.title') %></h1>-->
<h1>掲示板</h1>

<table>
  <!--<thead>-->
  <!--  <tr>-->
  <!--    <th>Text</th>-->
  <!--    <th>Contributor</th>-->
  <!--    <th>Email</th>-->
  <!--    <th colspan="3"></th>-->
  <!--  </tr>-->
  <!--</thead>-->

  <tbody>
    <% @contributions.each do |contribution| %>
      <tr>
        <td><span class="contribution-id"><%= contribution.id %></span>
            <span class="contributor"><%= contribution.contributor %></span>
        </td>
        <td><%= contribution.email %></td>
        <td><%= contribution.created_at.strftime("%Y/%m/%d(#{%w(日 月 火 水 木 金 土)[contribution.created_at.wday]})") %></td>
      </tr>
      <tr>
        <td colspan=3><%= contribution.text %></td>
      </tr>
      <tr>
        <td colspan=3>
          <hr>
        </td>
        <!--<td class="actions-link">-->
          <!--<%= link_to 'Show', contribution %>-->
          <!--<%= link_to 'Edit', edit_contribution_path(contribution) %>-->
          <!--<%= link_to 'Destroy', contribution, method: :delete, data: { confirm: 'Are you sure?' } %>-->
        <!--</td>-->
      </tr>
    <% end %>
  </tbody>
</table>

<div>
  <%= render 'form' %>
</div>

<!--<%= link_to 'New Contribution', new_contribution_path %>-->
